---
layout: single
title: Qwen3.5解析
categories:
  - AI
tags:
  - 网络模型
---

* content
{:toc}


源码：[qwen3.5](https://github.com/huggingface/transformers/tree/main/src/transformers/models/qwen3_5)

`Qwen3.5`包含了VIT进行图像处理，所以可以与`Qwen3-VL`做对比：

* RMSNorm稍有区别

* LinerAttention与FullAttention混合，比例是3:1

* LinearAttention与FullAttention加入了一步Gated操作



## RMSNorm

rmsnorm采用zero-centered rmsnorm，参考：https://github.com/huggingface/transformers/pull/29402，

也就是weight基础上会加1。

<!--more-->

## Linear Attention

![](https://harmonyhu.github.io/img/linear_attention.png)

### Cache的管理

用B代指`batch_size`，S代指`seq_len`.

FullAttention的Cache包括`key_cache`和`value_cache`，shape均为`[B,num_head,S,head_dim]`，对应`qwen3.5`的配置，具体为`[B,2,S,256]`.

LinearAttention的Cache包括`conv_states`和`recurrent_states`，shape分别为`[B, d_inner, d_conv]`和`[B, d_inner, d_state]`. 对应`qwen3.5`的配置，具体为`[B,12288,4]`和`[B,64,128,128]`.

### Prefill阶段

![](https://harmonyhu.github.io/img/linear_attn_prefill.png)

其中`chunk_gated_delta_rule`实现如下：

![](https://harmonyhu.github.io/img/chunk_gated_delta_rule.png)

### Decode阶段

![](https://harmonyhu.github.io/img/linear_attn_decode.png)

其中`recurrent_gated_delta_rule`实现如下：

![](https://harmonyhu.github.io/img/recurrent_gated_delta_rule.png)

`recurrent_state`更新算法如下：

$$
S_t = α_tS_{t-1} + β_tk_t(v_t-S^T_{t-1}k_t)^T \\
o_t = S^T_tq_t
$$

其中S对应`recurrent_state`, α对应g，β对应beta，k对应key，v对应value，q对应query.





